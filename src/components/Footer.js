/**
 * Footer component is a simple functional component
 * that displays a footer message.
 */

const Footer = () => {
    // Renders a heading (h3) containing the footer text
      return <h3>My Footer in another component</h3>;
    };
    
    export default Footer;