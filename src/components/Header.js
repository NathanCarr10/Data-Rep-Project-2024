/**
 * Header component is a simple functional component
 * that displays a header message.
 */

const Header = () => {
    // Renders a heading (h1) containing the header text
    return <h1>My Header in another component</h1>;
  };
  
  export default Header;